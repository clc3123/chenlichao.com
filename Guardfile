guard 'livereload' do
  watch(%r{^_site/.+$})
end

# guard 'sass', :input => '_sass', :output => 'css',
#   :all_on_start => true

# guard 'coffeescript', :input => '_coffee', :output => 'js', :bare => true,
#   :all_on_start => true
